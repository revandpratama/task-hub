package routes

import (
	"github.com/gofiber/fiber/v2"
)

func ProjectRoutes(r fiber.Router) {
	// taskRepo := repository.NewTaskRepository(database.DBCONN)
	// projectRepo := repository.NewProjectRepository(database.DBCONN)

	// service := service.NewProjectService(projectRepo, taskRepo)

	// handler := handler.NewProjectHandler(service)

	// r.Get("/projects", handler.GetAll)
	// r.Get("/projects/:id", handler.GetById)

	// r.Post("/projects", handler.Create)
	// r.Delete("/projects/:id", handler.Delete)
	// r.Put("/projects/:id", handler.Update)

}
