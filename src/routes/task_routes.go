package routes

import (
	"github.com/gofiber/fiber/v2"
)

func TaskRoutes(r fiber.Router) {
	// taskRepo := repository.NewTaskRepository(database.DBCONN)
	// taskAttRepo := repository.NewTaskAttachmentRepository(database.DBCONN)

	// service := service.NewTaskService(taskRepo, taskAttRepo)

	// handler := handler.NewTaskHandler(service)

	// r.Get("/tasks", handler.GetAll)
	// r.Post("/tasks", handler.Create)

	// r.Get("/tasks/:id", handler.Get)

	// r.Put("/tasks/:id", handler.Update)
	// r.Delete("/tasks/:id", handler.Delete)
}
